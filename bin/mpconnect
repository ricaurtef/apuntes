#!/bin/bash

ssh_connect () {
   echo "ssh_connect."
}

read -p "Enter server Ip > " ip

if [[ "$ip" =~ ^([0-9]{1,3}\.){3}[0-9]{1,3}$ ]]; then
   ssh_connect
else
   echo "Invalid input." >&2
   exit 1
fi
